package com.example.kwordle;

import android.os.Bundle;

import androidx.appcompat.app.AppCompatActivity;
import androidx.fragment.app.FragmentManager;

public class CorrectDialog extends AppCompatActivity {

    /*
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_main);
        showCorrectDialog(MainActivity.theAnswer);
    }

    private void showCorrectDialog(String answer){
        FragmentManager fm = getSupportFragmentManager();
        Correct correctDialog = Correct.newInstance(answer);
        correctDialog.show(fm, R.layout.correct);
    }
    */

}
